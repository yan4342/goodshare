server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8500
    gateway:
      httpclient:
        connect-timeout: 30000
        response-timeout: 300s
      discovery:
        locator:
          enabled: true
      routes:
        - id: crawler-service
          uri: lb://crawler-service
          predicates:
            - Path=/api/prices/**
        - id: goodshare-server
          uri: lb://goodshare-server
          predicates:
            - Path=/**
